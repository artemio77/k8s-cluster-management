repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami
releases:
  - name: redis
    namespace: redis
    chart: bitnami/redis
    version: 17.10.1
    installed: true
    values:
      - values.yaml