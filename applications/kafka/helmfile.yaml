repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami
releases:
  - name: kafka
    namespace: kafka
    chart: bitnami/kafka
    version: 22.0.1
    installed: true
    values:
      - values.yaml
